spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5432/repository
    username: postgres
    password: admin
utils:
  cart:
    prefix: SPRING_WEB_

